"use client";

/**
 * NotificationPrompt
 *
 * Push notifications are not yet implemented (no service worker, no VAPID keys).
 * This component is kept as a placeholder and returns null until the feature is
 * built out.
 */
export function NotificationPrompt() {
  return null;
}
