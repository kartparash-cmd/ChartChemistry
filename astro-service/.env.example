# ChartChemistry Astro Service Configuration

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Swiss Ephemeris data path (optional, uses bundled data if unset)
EPHE_PATH=

# Logging level
LOG_LEVEL=info
